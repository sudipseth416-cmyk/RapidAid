import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function HospitalSettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Alert thresholds, integration endpoints, and coordinator preferences."
    />
  );
}
